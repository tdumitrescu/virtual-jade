div
  - var foo = ['bar']
  .example(class=foo)

  - var baz = x + 1
  -
    var z = [
      1,
      2,
      3
    ]

  if baz
    .baz

  .inline-script= h('.raw-inline', 'within another div')
  = h('.raw-buffered', 'raw so raw')
  - h('.unbuffered', 'should not be output')
  .empty-code
    -

  .buffered-null= null
